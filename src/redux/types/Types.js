export const LoginTypes = {
  login: "login",
  logout: "logout",
};

export const RegisterTypes = {
  register: "register",
};
export const simuladorTypes = {
  showSimulador: "show",
  hideSimulador: "hide"
};

export const userTypes = {
  users: "users",
};
